'use strict'
exports.main=async event=>{const enterpriseId=String(event.enterpriseId||'');const month=String(event.month||'');if(!enterpriseId||!month)return{code:400,message:'统计参数不完整'};const db=uniCloud.database();const views=(await db.collection('product_views').where({enterpriseId,month}).get()).data;const orders=(await db.collection('orders').where({enterpriseId}).get()).data.filter(x=>{const d=new Date(x.createdAt);return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')===month});const uv=new Set(views.map(x=>x.userId)).size;const buyers=new Set(orders.map(x=>x.userId)).size;return{code:0,pv:views.length,uv,buyers,orders:orders.length,conversionRate:uv?Number((buyers/uv*100).toFixed(2)):0}}
